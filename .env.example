# 複製為 .env.local 後調整。所有變數皆為建置期常數，不含任何秘密。

# GitHub Pages project site 子路徑，格式 "/<repo-name>/"；本機開發或 user site 用 "/"。
VITE_BASE_PATH=/

# 資料模式：demo（載入 public/data/demo/trip.json）或 production（載入 public/data/trip.json）。
# production 模式找不到檔案或檔案標記為 demo 時會顯示錯誤，不會 fallback 到示範資料。
VITE_DATA_MODE=demo
