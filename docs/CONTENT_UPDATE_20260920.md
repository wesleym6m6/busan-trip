# 2026-09-20 行程內容更新

依使用者最新指示重新唯讀核對行程表，再完成網站內容與小幅資料結構調整。保留已定案配色、圖片、頁首海浪海鷗、四分頁、五天順序及本機儲存方式。原表與正式站尚未變更。

## 內容

- 補充 27 個場所的具體介紹，讓餐廳差別、招牌餐點、入口與使用規則比較容易理解。保留有意義的形容詞，不用排名或「必吃」取代資訊。
- 依表同步味贊王 11:30 Naver 已訂、Ribs 18:30、SPA 館內午餐、新世界購物清單、Luge 與午餐可互換。
- 李禹煥維持 10/6 16:00，收合卡直接提醒可能休館。沒有加入替代景點或改路線。
- 加入 19 個可省略的停留預算；其餘如航班、集合、退房不硬套固定時長。頁面說明排隊與交通另計。
- 待辦集中到工具頁，但開館疑慮、候車、緊接下站的取捨仍寫在行程卡。
- 查證更正包括水邊最高民樂本店使用 Tabling、WiFi Dosirak 金海櫃台 3 號出口、Ribs 屬韓式牛肋排；保留 DIART 不同開門版本的疑慮。
- 詳細來源與採用限制見 [來源紀錄](CONTENT_SOURCES_20260920.md)。

## 程式

- `places` 共用介紹與補充資料；行程只保留當次安排。
- 備案與地址以 `placeKey` 引用場所；修正同家店要改多處和第三選項重複。
- `visitMinutes` 以分鐘區間儲存，schema、語意驗證與測試同步更新。
- 修正時間解析，保留「11:00 前」與「15:50 台灣時間」。
- 沒有新增依賴、後端、Service Worker 或自動部署。

## 驗證

使用 Node 25 時，`NODE_OPTIONS=--no-experimental-webstorage VITE_BASE_PATH=/busan-trip/ npm run check` 包含 typecheck、lint、48 項測試、資料驗證、正式建置。新增行為測試已先驗證失敗，再確認修正後通過。

Chrome 真實瀏覽器以 360px／390px 觸控視窗驗收，兩個寬度結果相同：

| 項目 | 結果 |
|---|---|
| 五天日期、38 張可展開卡片逐一開／關 | PASS |
| 四個分頁、第五天備案空狀態、全部備案 | PASS |
| 行程、備案、工具共 30 個不同 Naver 搜尋連結的格式與搜尋詞 | PASS（未宣稱每個外站搜尋結果都重新驗收） |
| 行前待辦、地點快查與匯率換算 | PASS |
| 日期與打包勾選重新載入後保留 | PASS |
| 「11:00 前」「15:50 台灣時間」可見 | PASS |
| 各頁橫向溢出／失敗圖片／頁面或 console 錯誤 | 0／0／0 |
| 海浪 `layered-surf` 播放、連續切日 4 隻獨立海鷗、畫面像素變化、減少動態切換 | PASS |
| 本輪程式獨立審查 | 無 Critical／Important；已修正場所介紹混入當天安排的 Minor |

本機驗收地址為 `http://127.0.0.1:4175/busan-trip/`。JSON、截圖、執行腳本與 check log 留在此工作任務的 `outputs/busan-content-20260920/`；維護時以該次檔案為本機證據，不把歷史正式站驗證當作這次發布紀錄。

尚未發布 GitHub Pages；真實 iPhone Safari、實際旅行日期、交通／候位與離線啟動為 NOT TESTED。建置有 Zod 上游註解位置警告，未影響 typecheck、測試或 build。
